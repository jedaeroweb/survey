<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
  <meta charset="utf-8">
  <title><%=t(:service_name)%></title>
  <meta name="description" content="<%=t(:service_name)%>">
  <meta name="author" content="Sleeping-Lion">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <%= stylesheet_link_tag "front", "data-turbo-track": "reload" %>
  <link rel="icon" href="/favicon.ico">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>
<body>
<%= render :partial => 'layouts/header' %>
	<div id="mom" class="<% if @total_question.present? && @total_answered.present?  %>show_count<% end %>">
		<% if @total_question.present? && @total_answered.present?  %>
		<% if @total_question==@total_answered %>
		<h3 id="question_count">감사합니다 모든 문항(<%=@total_question %>)에 답변하셨습니다</h3>
		<% else %>
		<h3 id="question_count">총 <%=@total_question %>개의 문항 중 <b class="red"><%=@total_answered %></b>개의 문항에 답변하셨습니다</h3>
		<% end %>
		<% end %>
		<main id="main" class="container">
			<div class="sub_main">
			<%= render :partial => 'layouts/notice', :locals => { :notice => notice, :alert => alert } %>
				<%= render :partial => 'layouts/ad' %>
			<%= yield %>
			</div>
		</main>
	</div>
	<%= render :partial => 'layouts/footer' %>
	<div class="modal fade" id="myModal" tabindex="-1" role="dialog"></div>
<%= javascript_importmap_tags %>
</body>
</html>
